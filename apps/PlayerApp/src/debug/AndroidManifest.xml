<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android Visualizer requires RECORD_AUDIO even for own playback session (error -3 without it). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application>
        <activity
            android:name="by.tigre.debug_settings.DebugActivity"
            tools:node="remove" />

        <activity
            android:name="by.tigre.music.player.debug.PlayerAppDebugActivity"
            android:configChanges="orientation"
            android:exported="true"
            android:label="SettingsDebug"
            android:taskAffinity=".debug"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
